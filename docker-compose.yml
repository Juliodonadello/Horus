services:
  dashboard:
    image: grafana/grafana:7.4.0
    container_name: dashboard
    restart: always
    ports:
      - 3000:3000
    volumes:
      - grafana-data:/var/lib/grafana

  tsdb:
    build: ./tsdb
    restart: always
    container_name: metrics-db
    ports:
      - 8086:8086
    volumes:
      - ./metrics_db/config.yml:/config.yml

  db:
    image: postgres:12
    restart: always
    container_name: events-db
    environment:
      POSTGRES_PASSWORD: ccicevents
    volumes:
      - ./db/db-scripts:/docker-entrypoint-initdb.d

  adminer:
    image: adminer:4.7.9
    container_name: adminer-aux
    restart: always
    ports:
      - 4000:8080

  horus-api:
    build: ./api
    container_name: api-horus
    restart: always
    ports:
      - 8080:5000
    env_file:
      - api/horus-api.env

networks:
  default:
    external:
      name: horus-ccic

volumes:
  grafana-data:
   external: true
